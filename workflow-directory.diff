--- a/release-repos/sd-ppp/typescripts/modules/photoshop/src/tsx/WorkflowList.tsx
+++ b/release-repos/sd-ppp/typescripts/modules/photoshop/src/tsx/WorkflowList.tsx
@@ -30,6 +30,17 @@ const WorkflowList: React.FC<WorkflowListProps> = ({ setEditorMode }) => {
         showingList
     } = useSDPPPWorkflowList();

+    // Helper function to calculate parent directory path
+    const getParentDirectory = (currentPath: string): string => {
+        if (!currentPath || currentPath === '') return '';
+        const normalizedPath = currentPath.endsWith('/') ? currentPath.slice(0, -1) : currentPath;
+        const lastSlashIndex = normalizedPath.lastIndexOf('/');
+        if (lastSlashIndex === -1) return '';
+        return normalizedPath.substring(0, lastSlashIndex + 1);
+    };
+
+    const parentDirectory = getParentDirectory(currentViewingDirectory || '');
+
     let listReplacer = null;
     if (workflowsError)
         return <div className="workflow-list">
@@ -42,9 +53,9 @@ const WorkflowList: React.FC<WorkflowListProps> = ({ setEditorMode }) => {
                     currentViewingDirectory ?
                         <DirectoryItem
                             key={currentViewingDirectory}
-                            path="../"
-                            dirname={currentViewingDirectory.slice(0, -1)}
+                            path={parentDirectory}
+                            dirname="../"
                             onDirectorySet={(dirname: string) => {
-                                setCurrentViewingDirectory(dirname)
+                                setCurrentViewingDirectory(parentDirectory)
                             }}
                         />
                         : null
@@ -60,7 +71,7 @@ const WorkflowList: React.FC<WorkflowListProps> = ({ setEditorMode }) => {
                                 <DirectoryItem
                                     key={path}
                                     path={path}
-                                    dirname={currentViewingDirectory.slice(0, -1)}
+                                    dirname={path.split('/').pop() || path}
                                     onDirectorySet={(dirname: string) => {
                                         setCurrentViewingDirectory(dirname)
                                     }}